@model IEnumerable<MedOffice.Models.Patient>

<br /><br />
@using (Html.BeginForm("PatientSearch", "Search", FormMethod.Get))
{
    @Html.TextBox("searching")<input type="submit" value="Szukaj" />
}

<table class="table table-striped">
    <thead>
        <tr>
            <th>Imię</th>
            <th>Nazwisko</th>
            <th>Pesel</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() == 0)
        {
            <tr>
                <td colspan="3" style="color:red">
                    Brak wyników
                </td>
            </tr>
        }
        else
        {
            foreach (var item in Model)
            {
            <tr>
                <td>@item.Name</td>
                <td>@item.Surname</td>
                <td>@item.Pesel</td>
                <td style="text-align: center">
                    @Html.ActionLink("Edytuj", "../Patients/Edit", new { id = item.Id }) |
                    @Html.ActionLink("Szczegóły", "../Patients/Details", new { id = item.Id }) |
                    @Html.ActionLink("Usuń", "../Patients/Delete", new { id = item.Id })
                </td>
            </tr>
            }
        }
    </tbody>
</table>